To run this application:

1. Open terminal
2. Reading the training epression images  $python exhibition_fisher_emotion_classifier.py -update
3. Training the model  $python exhibition_fisher_emotion_classifier.py -train
4. Run the program $python exhibition_fisher_classifier.py

 
 
